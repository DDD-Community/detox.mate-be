spring:
  thymeleaf:
    encoding: UTF-8
  application:
    name: detoxmate
  profiles:
    default: local
    group:
      dev: dev
      local: local
      prod: prod

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    url: /openapi3.yaml
